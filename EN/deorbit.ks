parameter L is 174.7. run once lib. run mp_altin(20000,TTLng(L)). run exec. SAS ON. SET NAVMODE TO "Surface". SET SASMODE TO "Retrograde". SET WARP TO 4. WAIT UNTIL ALTITUDE <= BODY:ATM:HEIGHT. PANELS OFF. UNTIL STAGE:NUMBER = 0 {WAIT UNTIL stage:ready. stage.}.